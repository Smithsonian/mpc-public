version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
  commands:
    - pip install --upgrade pip
    - pip install -e "${READTHEDOCS_REPOSITORY_PATH}/mpc-client[all,docs]"
    - cd ${READTHEDOCS_REPOSITORY_PATH}/mpc-client && mkdocs build --site-dir $READTHEDOCS_OUTPUT/html
