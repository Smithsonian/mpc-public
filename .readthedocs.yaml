version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
  commands:
    - pip install mkdocs mkdocs-material "mkdocstrings[python]" mkdocs-jupyter
    - cd docs-public && mkdocs build --site-dir $READTHEDOCS_OUTPUT/html
